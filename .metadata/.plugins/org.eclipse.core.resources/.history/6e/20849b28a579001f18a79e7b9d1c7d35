impot.utpackage javaCurso;
imort java .util. Scanner;  
public class Juroscompostos {
// Instanciando uma classe para usa- la:Scanner sc+new Scanner(System.int);
	// Entrada do uuario :
	system.out.println("digite o  capital:");
	double capita=sc.nextdouble() ;
	// pegando a taxa de juros:
	system.out.prinyln ("qual é a taxa de juros:");
	// pegando o tempo=sc.nextint(Juroscompostos: 
	system.out.println("Digite o tempo em anos:");
	// Calculando os juros comopostos :
	double monte = capital *Math.pow(1+(taxa/100,tempo);
	//saida final:system.out.println("o monte final será de RS$ "+monte sc. classe);
	
}
